# Clustering

## Overview

Recall that clustering is a form of *unsupervised learning* - searching for patterns and structure in data without relying on class labels.

The goal of a clustering algorithm is to divide the input data set into subcategories - the clusters - such that points within a cluster ar
